package com.ojo.cyrus.enums;

/**
 * Settlement currency. Nomba is NGN-only today; the enum exists so amounts carry an explicit,
 * type-safe currency rather than a free-form string.
 */
public enum CurrencyCode {
    NGN
}
